# fcitx5 经典界面的主题选择。
#
# 注意：这两个键必须写在**文件顶层**，不能包在 [ClassicUI] 段里。
# fcitx5 的 ClassicUIConfig 是顶层配置，main options 直接读顶层键；
# 写成段落会被当成未知段整段忽略，Theme 于是回落到编译期默认值 "default"，
# 表现就是"换了皮肤但候选窗一直是默认主题"（实测 2026-09-14）。
Theme=nyxmellow
DarkTheme=nyxmellow
